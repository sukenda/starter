# Next milestone: authentication and RBAC

After this foundation is green, the next stacked PR owns database tables/migrations for users, roles, permissions, role-permission assignments, user-role assignments, refresh sessions, and menu authorization metadata; password hashing; login/refresh/logout/me endpoints; server-side permission middleware; OpenAPI contracts; and backend integration tests.

Login screens and admin visual implementation remain in their dedicated client milestone PRs.
