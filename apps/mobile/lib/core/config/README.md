# Mobile configuration

Build-time environment values are supplied with Dart defines, for example `--dart-define=API_BASE_URL=https://api.example.com`. Secrets must never be compiled into the application. Environment configuration is for public runtime endpoints and feature flags only.
