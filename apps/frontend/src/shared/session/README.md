# Frontend session boundary

This package represents the authenticated identity and permission snapshot needed by routing and presentation. It does not make authorization decisions for the system: protected operations must always be authorized by the backend. UI permission checks only hide or disable unavailable interactions.
